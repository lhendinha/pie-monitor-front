export interface EtiquetasDeSubgrupoProps {
  /** Os nomes já resolvidos. Id cru é aceitável aqui -- é o que sobra quando
   * o subgrupo foi apagado, e mostrar algo é melhor que a etiqueta sumir. */
  nomes: string[];
}
