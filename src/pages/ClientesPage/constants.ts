/** Cabeçalhos da tabela de Clientes, na ordem do artifact.
 *
 * Aqui e não dentro do componente: é dado. Quando a tabela ganhar ordenação
 * por coluna, é esta lista que vira a fonte da chave de ordenação.
 */
export const COLUNAS_CLIENTES = ["Nome", "Documento", "Contato", "Processos"] as const;
