/** Abas do perfil. Só uma existe: no artifact é `<button class="tab-btn active">Identificação</button>`,
 * literalmente uma só.
 *
 * Fica como lista porque a aba já está desenhada e nomeada -- quando
 * aparecer "Notificações" ou "Sessões", entra aqui e mais nada muda. */
export const ABAS_DO_PERFIL = [{ id: "identificacao", rotulo: "Identificação" }] as const;
