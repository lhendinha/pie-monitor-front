/** As quatro contagens dos cartões da prévia. */
export interface ResumoDaPreviaProps {
  encontrados: number;
  /** Os marcados AGORA -- é o que o segundo cartão conta. */
  marcados: number;
  jaExistem: number;
  noutroSubgrupo: number;
}
